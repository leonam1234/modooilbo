# 세션 인계서 — 모두일보

> **새 세션은 이 파일부터 읽으십시오.** 마지막 갱신 2026-09-02.
> 역할명은 **`모두일보 개발 및 배포 담당자`**입니다. 종전 Claude 모두일보 총괄·개발담당
> 업무와 사이트 코딩·유지보수도 이 역할이 이어받습니다.
>
> ✅ **2026-09-02 최신 지시: 유수화님이 상시 자동 배포를 승인했습니다.** 2026-09-01의
> 패키지별 명시 승인 대기 규칙은 이 지시로 대체합니다. 콘텐츠 제작 작업 인수 → 1차 정적·동적
> 게이트 → `모두일보 독립 리뷰 담당` 기사별 검수 → 결과 회수·수정분 재검수 → 최종 게이트를
> 지키고, PASS·HOLD 0이면 별도 승인 질문 없이 CMS 변환, `content/articles/` 반영, Git 커밋,
> 빌드, Preview, Production, 라이브 검증까지 이어서 수행합니다. 리뷰·게이트가 HOLD이면 자동
> 배포하지 않으며, 사용자의 현재 중지·보류 지시는 언제나 이 상시 승인보다 우선합니다.
> 이 파일은 현재 세션 진입점과 실행 체크리스트입니다. 우선순위는 **유수화님의 현재 명시 지시 →
> 발행 거버넌스 `wiki/09-publishing.md` → 배포·승급 `wiki/06-deployment.md` → 세부 사례
> `wiki/operations/04-publishing-protocol.md`**입니다. 구현 사실은 현재 코드와 대조합니다.
> 2026-08-28 감사(취재 비율 목표의 근거 · AI 지문) 결과는
> [`wiki/audits/2026-08-28-취재비율-근거와-AI지문.md`](wiki/audits/2026-08-28-취재비율-근거와-AI지문.md)
> 에 있습니다. **그 문서 §5에 "검증에서 무너진 주장" 목록이 있으니 다시 꺼내지 마십시오.**

## 0. 먼저 확인할 것 (30초)

```bash
cd ~/GIT/modooilbo
git log --oneline -3
git status --porcelain          # 비어야 정상
git fetch -q origin master && git rev-list --left-right --count HEAD...origin/master  # "0 0" 이어야 정상
ls content/articles/$(date +%Y-%m-%d)-*.md 2>/dev/null | wc -l             # 오늘 발행분
```

**다른 세션이 같은 저장소를 만지고 있을 수 있습니다.** 하루가 지났으면 커밋이
쌓여 있는지 반드시 먼저 보십시오. 모르고 손대면 충돌합니다.

## 1. 패키지 경로와 파일 권한

**2026-08-28부터 운영정보가 전부 깃허브에 있습니다**(오너 지시 — "다른 개발자도 알 수 있게").

| 저장소 | 공개 | 무엇이 |
|---|---|---|
| `leonam1234/modooilbo` | **공개** | 사이트 코드·기사·이 인계서·wiki |
| `wlashvpel/modooilbo-packages` | 비공개 | 원고 패키지·코덱스 전달문·HOLD사유 (로컬 `~/GIT/modooilbo-packages/`) |
| `wlashvpel/modooilbo-codex` | 비공개 | 콘텐츠 총괄 정본·파이프라인 (로컬 `~/Documents/Codex/모두일보/`) |

⚠️ **운영 문서를 공개 저장소에 새로 넣지 마십시오.** 제작 파이프라인·검수 기록은
포털 제휴 심사에서 「자동생성기사」 판정 근거가 될 수 있습니다(구 제평위 규정 별표 1 단서 —
감사 §2-4). 새 운영 문서는 위 두 비공개 저장소에 넣습니다.

⚠️ **비공개 저장소 둘은 사본이 아니라 그 폴더 자체입니다.** 작업 경로는 종전 그대로이고
커밋만 하면 됩니다. 사본을 따로 만들지 마십시오 — 반드시 어긋납니다.

**코덱스 원고 패키지는 `~/GIT/modooilbo-packages/` 에 있습니다.**

```
~/GIT/modooilbo-packages/
  출고대기/       아직 발행하지 않은 것 (2026-08-26 현재 보류자료만 있음, 신규 패키지 0)
  발행완료/       발행이 끝난 것 (2026-08-26 기준 77개)
  아카이브_미발행/ 발행하지 않았고 앞으로도 안 할 것 (마감 만료·HOLD 미해소)
  참고문서/       원고가 아닌 회의·발주·정리 기록
```

⚠️ **`출고대기/보류자료/`는 최상위 flat 폴더입니다** — 개별 원고 md·jpg·HOLD사유.md를
그대로 둡니다. 최근 패키지(8/25~)는 패키지 내부에 `보류자료/`를 자체적으로 두는데,
그 원고가 발행되지 않았다면 패키지를 통째로 `발행완료/`로 옮기기 **전에** 반드시
이 최상위 폴더로 꺼내십시오. 안 그러면 "발행한 적 없는 원고를 발행완료에 넣지
말라"는 아래 규칙을 어기게 됩니다(8/26에 김창완밴드 건에서 실제로 이렇게 됐다가
발견해 옮겼다).

⚠️ **발행한 적 없는 원고를 `발행완료/` 에 넣지 마십시오.** 기록이 거짓이 됩니다.
못 내보낸 것은 `아카이브_미발행/` 으로 보내고 사유를 README 에 적습니다.

⚠️ **2026-08-28부터 git 저장소입니다**(`wlashvpel/modooilbo-packages`, 비공개).
종전 규칙("git 명령을 실행하지 마십시오")은 **폐기**됐습니다. 패키지를 옮기거나
전달문을 쓴 뒤에는 커밋·푸시하십시오.
추적 대상은 `*.md`·`*.txt` 뿐입니다 — 이미지(약 2GB)와 검증 증빙(pdf·hwp 등 약 250MB)은
`.gitignore` 로 빠집니다. **지운 게 아니라 로컬에 그대로 있습니다.**

발행이 끝나면 해당 패키지를 `발행완료/` 로 옮기십시오. 출고대기에는 아직
안 나간 것만 남습니다.

### macOS 파일 권한 — 2026-08-28 복구 확인

2026-08-21 에 macOS TCC 가 접근 권한을 회수했다는 기록이 있었으나, **2026-08-28
세션에서 실측하니 전부 살아 있습니다.**

| 경로 | 읽기 | 쓰기 |
|---|---|---|
| `~/GIT` | OK | OK |
| `~/Documents` (`~/Documents/Codex/모두일보/` 코덱스 정본 포함) | OK | OK |
| `~/Downloads` | OK | OK |

**코덱스 정본을 직접 열어 대조할 수 있습니다.** 8/28 의 정본 중복 정리(§3-B)가
그래서 가능했습니다. 기자 사진도 `~/Downloads` 에서 바로 읽을 수 있습니다.

⚠️ **패키지는 `~/GIT/modooilbo-packages/` 에 그대로 둡니다.** 권한이 복구됐다고
다시 옮기지 마십시오 — 경로가 여러 문서에 박혀 있고, 옮겨서 얻을 것이 없습니다.

**`deployments/deploy-log.jsonl` 쓰기 EPERM 도 해소됐습니다.** 8/28 배포에서 정상
기록됐습니다. 그 에러가 다시 나오면 권한이 또 회수된 것이니 위 표를 다시 실측하십시오.

## 2. 열려 있는 작업

- ~~집필 지침이 본문에 인쇄된 기사~~ **완료(8/28) — 13편 수리 + 빌드 게이트 3층 신설.**
  감사가 지목한 2편이 아니라 **13편**이었다(감사의 grep 이 어미 두 개만 봤다).
  `scripts/build-content.mjs` 가 이제 현재형 규칙문을 잡아 빌드를 끊는다.
  ⚠️ **면책 고지 88편은 통과한다** — 과거형·부정형(`…아니다`·`…않는다`)은 투명성
  고지라 일부러 살렸다. `이 기사는` 을 통째로 막지 마라.
  ⚠️ 게이트 어미 목록에 `밝혀야 한다`·`붙여야 한다`를 넣지 마라 — 사설이 기관에
  요구하는 문장과 공모전 제출 규정이 매일 걸려 경고가 소음이 된다.
- ~~AI 지문 정리 4건~~ **완료(8/28)** — 본문 KST·취재로그는 42편 직접 정리했고,
  발행 시각 분산·리드 문단·소제목 어휘는 코덱스 전달문으로 넘겼다:
  `~/GIT/modooilbo-packages/코덱스_전달_20260828.md`. **회신 확인이 남아 있다.**
- ~~기자 사진~~ **완료(8/21)** — 편집국 6명 전원 실사진. 이니셜 아바타 0개.
  새 기자 추가 시: 사진을 `public/reporters/<slug>.jpg` 로 512px 정사각 크롭 후
  `src/lib/reporters.ts` 에 `photo: "/reporters/<slug>.jpg"` 추가.
  ⚠️ 원본 파일을 지우기 전에 크롭 결과를 눈으로 확인할 것 — 상반신 사진은
     얼굴이 작게 잡혀 재크롭이 필요한데, 원본을 지우면 소재가 없어진다.
- 🔴 **애드센스 — 반복 비승인. 기술 원인은 전부 배제됐다.**
  2026-08-30 「조치 필요」 메일 수신, 콘솔에 고칠 항목 없음, 오너 진술 "맨날 저 메일".
  ads.txt·robots·광고코드·필수페이지·사이트맵 전부 정상 확인(9/1). **여기를 다시 파지 마라.**
  ⚠️ `pub-1741876528103024` **한 계정에 modooilbo·rider-sesang 둘 다** 걸려 있어 함께 평가된다.
  실측: 직접 인용 **28편(2%)** · 보도 동사 32% · 반복 소제목(「한 문장 결론」235회) ·
  1,253편 전부 AI 고지. 리뷰어 눈엔 "취재 없이 공개 자료를 옮긴 사이트"다.
  → 전문·판정·해야 할 일: [`wiki/audits/2026-09-01-애드센스-비승인-원인분석.md`](wiki/audits/2026-09-01-애드센스-비승인-원인분석.md)
  ⚠️ **지금 재신청하지 말 것.** 같은 메일이 또 온다.
- **빙 재크롤** — **8/21 14:10 제출 완료, 결과 대기 중.** 담당 에이전트가 처리했고
  지시문은 `~/GIT/modooilbo-packages/빙_재크롤_지시_20260821.md` 에 있다.
  - URL 6건(`/`·`/policy/`·`/about/`·`/newsroom/`·`/reporter/yoo-seunghyun/`·
    `/reporter/choi-eunyoung/`) 색인 요청 완료. **당일 할당량 6 소진 / 94 잔여.**
  - `sitemap.xml` 재제출 완료, 처리 중.
  - 전체 999페이지 사이트 검사는 **대기 중**.
  ⚠️ **meta description 57건 지적은 아직 "해소"로 기록하지 않았다.** 수정 자체는 끝났지만
  사이트 검사 결과가 나오기 전이라 확인된 사실이 아니다. 검사 결과를 보고 이 항목을 닫을 것.
  ⚠️ **모르고 다시 제출하지 말 것** — 할당량만 깎인다. 결과부터 확인한다.
  ⚠️ IndexNow 키·배포 코드는 건드리지 않았다(그대로 두는 것이 맞다 — 아래 참조).
- 🔴 **메일 발송 설정**(오너) — **애드센스까지 걸린 최우선 병목이다.**
  회신이 곧 인용문이라 인용 2%를 올리는 유일한 경로다(위 감사 §5-1). 직접취재 질의용. 현재 ImprovMX 수신
  포워딩만 있고 발송 불가. ImprovMX SMTP 릴레이 또는 Google Workspace 필요.
  발송 명의는 `newsroom@modooilbo.com`.
  ⚠️ **이게 direct 비율 미달의 병목이다.** 메일을 못 보내니 기관 질의가 안 되고,
  질의가 없으니 direct 기사가 안 나온다(8/23·8/24 direct 0편). 우선순위가 올라갔다.

- **직접취재 `direct` 편수 부진** — 7일 이동 **2.4%**(8/28 실측). 8/22 이후 거의 `direct 0편`.
  ⚠️ **"비율 붕괴"라고 쓰지 말 것.** 2026-08-28 감사에서 확인했다 — 법·포털의 「자체기사」
  정의는 우리 `direct` 보다 훨씬 넓어(보도자료 재분석·재구성 포함) **2.4%는 법정 30%
  미달을 뜻하지 않는다.** 목표치 처리는 §3-A 결정 대기 중이다.
  ```
  8/21 25.0% → 8/22 13.0% → 8/23 0.0% → 8/24 0.0% → 8/25 0.0% → 8/26 0.0%
  ```
  분류는 정직하다(코덱스가 과대 분류 거부). 구조 문제다 — 원고 작성 시점에 direct
  소재가 없다. **전날 미리 질의를 걸어두는 운영 변경**과 위 메일 발송 설정이 필요하다.
  포털 심사에서 자체기사 비율 근거로 쓰이는 지표라 방치하면 안 된다.
  집계: `node scripts/reporting-report.mjs`
  📘 **실무 정본은 코덱스 쪽입니다: `직접취재_분류_증빙_정본.md`**
     (`wlashvpel/modooilbo-codex` 비공개 저장소 · 로컬 `~/Documents/Codex/모두일보/`)
     (저장소의 `wiki/operations/05-direct-reporting.md` 는 중복이라 8/28 폐기).
     코덱스가 갖고 있지 않던 3가지(게이트 충돌→질의 대상 매핑 · 이해관계 고지 실사례 ·
     발송 계정 병목)는 `~/GIT/modooilbo-packages/코덱스_전달_20260828.md` §5 에 있다.
     ⚠️ 보류자료 9건 중 5건은 **기관에 물어야만 풀리는 충돌**이라, 그 질의가 곧 direct다.
  ⚠️ 8/26 패키지부터 코덱스가 "직접취재_질의후보_N건.md"를 패키지에 동봉하기
  시작했다(기관·질문·공식 문의창구까지 준비, 상태는 "질의준비"). 메일 발송이 세팅되면
  이 목록으로 바로 질의를 걸 수 있다.

- **보류자료 처리 대기** — `~/GIT/modooilbo-packages/출고대기/보류자료/`
  - `2026-08-22-lee-eungyeol-…` — 8/22 개막 공연이라 **시의성 만료 추정**. 폐기 여부 코덱스 확인 필요
  - `2026-08-23-oedipus-…` / `2026-08-23-passi-…` / `2026-08-23-tropical-storm-moke-…`
  - `2026-08-26-kim-changwan-band-sync-next-…` — 공식 러닝타임 120분/70분 표기 충돌이
    RFE/RFF/RFG 재검수에서도 해소되지 않아 격리. 같은 자리는 "마음을 담은 클래식"으로
    교체 발행됨(8/26). research/PNG는 `발행완료/2026-08-26_오늘기사_24건/보류자료/`에 남음.
  각 건 사유는 같은 폴더의 `*_HOLD사유.md`. 코덱스가 교정해 재제출할 몫이다.

## 3. 결정 대기 (오너 판단 필요, 임의 진행 금지)

**2026-08-21 자 3건은 결정·구현 완료(아래 취소선).**
**2026-08-28 자로 아래 2건이 새로 열렸습니다.**

- ~~**A. `reporting-report.mjs` 목표치**~~ **결정: 법정 기준 병기 + 내부 목표 20%→10%,
  2026-08-28 구현.** `자체기사(법·포털 기준) = (direct+desk) ÷ 분류된 전체` 를 따로
  찍는다. 지금 100%다.
  ⚠️ **7일 이동 2.4% 를 「법정 30% 미달」로 읽지 마라.** 법·포털의 「자체기사」는
  보도자료 재분석·재구성까지 포함해 우리 `direct` 보다 훨씬 넓다. 다른 자로 잰 값이다.
  ⚠️ 인용할 때 단서를 빼지 마라 — 법정 30%는 심사 절차가 없고, 제평위는 2023.5.22
  활동 잠정 중단(해체 아님), 카카오 30%는 2025년 회차 기준, 네이버 임계값은 미공개다.
  근거·단서 전문은 `scripts/reporting-report.mjs` 머리 주석에 박아 뒀다.
- ~~**B. 정본 중복 정리**~~ **결정: 폐기, 2026-08-28 완료.**
  `wiki/operations/05-direct-reporting.md` 를 지우고 3가지만 코덱스 전달문으로 넘겼다.
  정본은 `wlashvpel/modooilbo-codex`(로컬 `~/Documents/Codex/모두일보/`) 한 곳이다.
  ⚠️ 8/28 저녁에 그 폴더를 git 저장소로 만들었다 — 종전엔 정본이 로컬에만 있어
  다른 개발자가 볼 수 없었다. 폐기 결정 자체는 유효하되, 접근 문제는 이걸로 풀렸다.
- ~~**AI 고지 방향**~~ **결정: 최소(B-1) 유지, 2026-08-21 구현.**
  ⚠️ 종전 이 항목의 서술("운영정책의 AI 활용 고지는 제거된 상태")은 **사실과 달랐습니다.**
  실제로는 모든 기사 하단에 고지 배너가 살아 있었고(`article/[slug]/page.tsx`),
  그 배너가 `/policy` 를 가리키는데 정작 그 페이지에 AI 항목이 없는 **끊긴 참조**였습니다.
  `/policy` 에 7절 「AI 활용 고지」를 신설해 해소했습니다. 화면 수위는 종전 그대로입니다.
  발행량을 설명할 근거를 화면에 두는 안(B-2 = `/transparency` 에 발행 체계 공개)은
  **채택하지 않았습니다** — 포털 심사를 본격 준비할 때 다시 꺼낼 카드입니다.
- ~~**홈 히어로 포지셔닝**~~ **결정: D-2(서브리드 혼합), 2026-08-21 구현.**
  리드 1건과 속보 티커는 종합뉴스 전용 유지, **보조 4칸만 사업 축 2 + 종합 2**.
  `/about` 미션에 기업 데이터 정체성을 넣었습니다.
  ⚠️ `isGeneralNews` 필터를 통째로 걷어내는 안(D-3)은 **채택하지 않았습니다.**
  정부지원금 공고가 대문 톱을 상시 점유하게 되므로, 되돌리기 쉬운 D-2 를 먼저 운영합니다.
- ~~**startup 배정**~~ **결정: C-1(startup↔labor 교환), 2026-08-21 구현.**
  유승현 `society·startup` / 최은영 `world·labor`. 근거는 §4.

## 4. 편집국 로스터 (2026-08-20 재편)

```
명단 6명   김영환 경제부 · 유승현 사회부 · 남동균 문화·스포츠부
           박유주 논설위원 · 서영호 공공입찰 전담 · 최은영 국제부
명단 제외  김성우(대표이사·발행인) · 유수화(고충처리인)
```

⚠️ **제외 2명의 프로필 페이지는 지우지 마십시오.** 기명 기사 153편이
바이라인에서 그 주소로 링크하고 사이트맵에도 있습니다. `listed: false` 로
목록 노출만 분리한 상태입니다.

⚠️ **유수화 명의로 새 사설·칼럼을 발행하면 안 됩니다.** 고충처리인이 자기 글의
고충을 처리하는 구조가 됩니다. 논설은 박유주 명의입니다.

일일 24편 = 12개 카테고리 × 2편, 6명 × 4편 균등:

```
economy·deals 김영환 | society·startup 유승현 | bids·grants 서영호
culture·sports 남동균 | opinion·industry 박유주 | world·labor  최은영
```

**2026-08-21 변경 — startup ↔ labor 를 맞바꿨습니다.** 993편 전수 저자 분포를 보면
8/20 재편의 이동은 world(김성우·발행인)·opinion(유수화·고충처리인) 승계, grants·bids
(김영환 4축 과부하 분산)까지 전부 정합인데, **startup 75편만 현직 기자의 단일 저자
아카이브를 다른 현직에게 넘긴 사례**였습니다. 최은영은 world 하나만 정당하게 승계하므로
2축 균등을 맞추려면 두 번째 축이 필요하고 3축 아카이브 보유자는 유승현뿐이라 그에게서
받는 구조는 맞습니다. 다만 labor 는 이미 유승현 65 + 김영환 12로 복수 저자 이력이 있어
넘길 축으로 startup 보다 적합합니다.

⚠️ **과거 기사 바이라인은 소급 수정하지 않습니다.** 배정은 앞으로의 발주에만 적용됩니다.

## 5. 발행 절차

```bash
# 먼저 깨끗한 비-master 릴리스 worktree로 이동한 뒤 실행
set -euo pipefail   # 아래 게이트 하나라도 실패하면 즉시 중단
MODOO_RELEASE_BRANCH="$(git branch --show-current)"
[[ -n "$MODOO_RELEASE_BRANCH" && "$MODOO_RELEASE_BRANCH" != master ]]
test -z "$(git status --porcelain)"
# ── 상시 자동 배포 승인 적용: 아래 1~4의 검수·게이트는 생략하지 않음 ──
# 1) 콘텐츠 제작 작업 인수 + 1차 정적·동적 게이트
#    원고·JPG·PNG 수량, slug, 보류자료, 검수문서, 공식 원문·접수화면·동적 값
# 2) `모두일보 독립 리뷰 담당`(Codex 작업 ID 01a05a60-a035-7921-8154-7aa4a7024f31)에 기사별 검수 요청
# 3) 기사별 reviewedBy·reviewedAt·reporterInsight, PASS/HOLD, 수정 요구를 회수
#    수정분 재검수 + §6 최종 게이트 완료
# 4) PASS/HOLD·수정·미확인 근거를 기록. HOLD 0이면 별도 승인 질문 없이 5단계로 진행

# ── 독립 리뷰와 최종 게이트 PASS·HOLD 0이면 자동 수행 ──
# 5) CMS 규약 검사·반영
MODOO_PACKAGE_DIR="/absolute/path/to/approved-package"   # 실제 승인 패키지 경로로 교체
MODOO_COMMIT_MSG_FILE="/absolute/path/to/commit-message.txt"
cp "$MODOO_PACKAGE_DIR"/articles/**/*.md content/articles/
cp "$MODOO_PACKAGE_DIR"/images_1200x675_jpg/*.jpg public/stock/
node scripts/editorial-quality-report.mjs --strict
node scripts/reporting-report.mjs
npm run build   # prebuild가 content·WebP·trending 생성까지 수행
git status --short
git add -- content/articles public/stock src/lib/content.generated.ts \
  src/lib/newest.generated.ts src/lib/trending-data.generated.json \
  src/lib/webp-manifest.generated.json
git diff --cached --name-status
git commit -F "$MODOO_COMMIT_MSG_FILE"   # 한글은 -m 대신 -F 로
git fetch -q origin master
MODOO_BASE_SHA="$(git rev-parse origin/master)"
MODOO_RELEASE_SHA="$(git rev-parse HEAD)"
git merge-base --is-ancestor "$MODOO_BASE_SHA" "$MODOO_RELEASE_SHA"
MODOO_ARTICLE_PATHS=(${(f)"$(git diff --diff-filter=AM --name-only \
  "$MODOO_BASE_SHA" "$MODOO_RELEASE_SHA" -- 'content/articles/*.md' \
  | sed -nE '/\/_/d; s#^content/articles/(.*)\.md$#/article/\1/#p')"})
(( ${#MODOO_ARTICLE_PATHS[@]} > 0 ))   # 0건이면 기사 패키지 승급 중단
print -l -- "${MODOO_ARTICLE_PATHS[@]}" # 승인 범위·건수와 전건 대조
# 6) Preview 선검증 — master가 아닌 깨끗한 release 브랜치에서 실행
npm run deploy:preview
MODOO_PREVIEW_URL="$(node -e '
const fs = require("fs");
const rows = fs.readFileSync("deployments/deploy-log.jsonl", "utf8").trim().split("\n").reverse();
for (const line of rows) { try { const r = JSON.parse(line); if (r.env === "Preview" && r.commit === process.argv[1] && r.url) { process.stdout.write(r.url); process.exit(0); } } catch {} }
process.exit(1);
' "$MODOO_RELEASE_SHA")"
[[ "$MODOO_PREVIEW_URL" == https://*.pages.dev* ]]
npm run smoke -- "$MODOO_PREVIEW_URL" / /policy/ /newsroom/ "${MODOO_ARTICLE_PATHS[@]}"
# smoke-shots/<실행시각>/compare-*.png를 눈으로 대조하고 FAIL이면 운영 배포 중단
# 자동 FAIL 판정은 사이트 JS pageerror와 본문 100자 미만뿐이다.
# HTTP 상태·canonical·index/follow·OG·이미지 응답은 별도로 전건 확인한다.
# 7) Preview PASS 뒤 검증한 정확한 커밋을 master로 올리고 원격 SHA를 대조
test "$(git rev-parse HEAD)" = "$MODOO_RELEASE_SHA"
test -z "$(git status --porcelain)"
git push origin HEAD:master
git fetch -q origin master
MODOO_REMOTE_MASTER="$(git ls-remote origin refs/heads/master | awk '{print $1}')"
test "$MODOO_RELEASE_SHA" = "$MODOO_REMOTE_MASTER"
test "$MODOO_RELEASE_SHA" = "$(git rev-parse origin/master)"
# Production 호출 직전 원격을 한 번 더 읽어 Preview 검증 SHA가 바뀌지 않았는지 확인
test "$MODOO_RELEASE_SHA" = "$(git ls-remote origin refs/heads/master | awk '{print $1}')"
# 검증한 release 브랜치 HEAD가 원격 master와 정확히 같을 때만 브랜치 가드를 통제 우회
npm run deploy:prod -- --force-branch
# 8) 운영 라이브 재검증 — 신규 기사 경로 전건까지 엔진·뷰포트 4조합으로 확인
npm run smoke -- https://modooilbo.com / /policy/ /newsroom/ "${MODOO_ARTICLE_PATHS[@]}"
# HTTP·XML·OG 직접 검사는 아래 §9의 curl 예시로 별도 확인한다.
# 9) IndexNow 자동 통지 결과를 기록한다. 요청 접수와 실제 색인 완료를 구분한다.
# 10) 필수 색인 인계 — 오늘 신규 발행 기사 전건의 운영 canonical URL을 추출한다.
#    HTTP 200·self-canonical·index/follow를 확인한 URL만 아래 색인 담당자 작업으로 보낸다.
#    codex://threads/019ef3e0-e684-7be0-a164-3cdfacfeb6fa  (`색인 담당자`)
#    전달문에는 발행일·URL 전건·건수·사이트 커밋·기존 IndexNow 접수 결과를 포함한다.
#    색인 담당자는 엔진별 기존 증빙을 대조해 중복 요청을 피하고 Google·Naver·Bing 상태를 기록한다.
# 11) ⚠️ 오너에게도 오늘 기사 링크 전건을 목록으로 준다 — 매번 요구하신다.
```

⚠️ **2026-09-02 상시 자동 배포 승인은 독립 리뷰·최종 게이트를 없애지 않습니다.** 검수 뒤 기사
내용이 바뀌면 변경 기사를 독립 리뷰와 최종 게이트에 다시 보내고, 다시 PASS·HOLD 0일 때만
자동 배포합니다. 사용자가 특정 릴리스를 중지·보류하면 그 지시를 우선합니다.

⚠️ **발행 완료 보고 전에 색인 담당자 인계를 누락하지 마십시오.** 작업 메시지 전달은
`handoff_sent` 증거일 뿐 `requested`·`submitted`·`indexed_current` 증거가 아닙니다.
전달 시각과 URL 건수를 발행 결과에 남기고, 실제 색인 상태는 색인 담당자 결과를 따릅니다.

⚠️ **배포 직후 첫 요청은 엣지 캐시로 404 가 뜰 수 있습니다.** 20초 뒤
`Cache-Control: no-cache` 로 재확인하십시오. 실제 누락과 구분해야 합니다.
`no-cache` 를 붙여도 404 가 나는 경우가 있습니다(8/24 실제 발생). 실제 누락인지는
`out/article/<slug>/index.html` 존재 여부로 판정하고, 있으면 잠시 뒤 다시 부르십시오.

⚠️ **`${#var}` 길이는 셸과 locale에 따라 해석이 달라질 수 있습니다.** UTF-8 bash·zsh는
보통 한글 문자 수를 세지만, 바이트 수 도구와 섞으면 결과가 달라집니다. 메타 설명 길이는
사이트가 쓰는 JavaScript 기준으로 다시 계산하십시오.

⚠️ **React 주석**(`<!-- -->`)이 문자열을 쪼갭니다. HTML 검증 전에
`perl -0777 -pe 's/<!--.*?-->//gs'` 로 제거하십시오.

⚠️ **grep 이 결과 0줄이면 "없다"가 아닐 수 있습니다.** 소스에 raw 제어 바이트가
있으면 grep 이 그 파일을 바이너리로 보고 **에러 없이 침묵**합니다. 앞으로 grep 결과가
수상하게 비면 `file <경로>` 로 판정을 보고 `grep -a` 로 다시 확인하십시오.
`scripts/build-content.mjs` 가 8/21 에 그래서 "reporting 검증이 미구현"으로 보였는데,
**그때 바이트가 다 지워지지 않았습니다.** 8/28 에 마지막 한 쌍을 찾아 제거했습니다 —
하필 *이 함정을 경고하는 주석 자신*이 `\u0001`·`\u0000` 을 raw 로 갖고 있었습니다.
지금은 `file` 이 `UTF-8 text` 로 판정하고 `grep` 이 정상 동작합니다.

### 모바일 4조합 검증 — `npm run smoke`

엔진 2종(Chromium·WebKit) × 뷰포트 2종(402×874·402×660). 2026-08-28 도입.
도구 원본은 `wlashvpel/mobile-smoke`(공개)입니다. 현재 스크립트는 네 조합 모두 iPhone
Safari UA를 쓰고 `app`·`browser`는 높이만 모사합니다. 따라서 이는 **엔진·뷰포트 비교**이며
실제 Android UA, 설치 PWA 상태, 주소창·키보드·인앱브라우저 UI의 실기기 검증이 아닙니다.

```bash
npm run smoke -- https://modooilbo.com / /policy/ /newsroom/
```

**서드파티(광고·분석) 에러는 세지 않습니다.** 애드센스가 사파리 교차출처 정책에
걸려 webkit 전 페이지에서 에러를 내는데, 그걸 FAIL 로 세면 **광고를 붙인 우리 사이트는
자동 게이트가 항상 막혀** 도구를 못 씁니다. 2026-08-28 에 필터를 넣어 올렸습니다
(원본 저장소에도 반영 — `wlashvpel/mobile-smoke` `1d079d8`).

```
적용 전  webkit 8/8 FAIL (전부 애드센스 교차출처)
적용 후  PASS · exit 0    ✓ webkit-app-home text=5857자 | (서드파티 3건 무시)
```

⚠️ **무시 목록을 함부로 넓히지 마십시오.** 넓힐수록 진짜 버그가 빠져나갑니다.
`TypeError`·`ReferenceError`·하이드레이션 오류는 지금도 전부 FAIL 입니다(역방향 시험 확인).

⚠️ **`결론:` 줄만 믿지 마십시오.** 레이아웃 깨짐은 JS 에러를 안 냅니다.
`compare-*.png`는 긴 뷰포트의 Chromium↔WebKit만 합성하므로 이를 눈으로 대조하고,
짧은 660px 뷰포트는 조합별 원본 PNG도 따로 확인하십시오.

⚠️ **prod URL 로 돌리면 "배포 후 검증"입니다.** 운영 배포 전에는 반드시
`npm run deploy:preview` 로 먼저 올리고 그 URL을 검사한 뒤, PASS와 `compare-*.png`
육안 대조를 모두 마친 경우에만 `npm run deploy:prod`로 승급하십시오.

⚠️ 브라우저 바이너리가 없거나 버전이 안 맞으면 `npx playwright install chromium webkit` 을
먼저 돌리십시오.

## 6. 발행 직전 게이트 — 반복되는 실패 유형

**코덱스 판정이 HOLD 0 이어도 게이트에서 걸립니다.** 8/19~8/21 사흘 연속
같은 유형이 나왔습니다. 원출처를 직접 열어 확인하십시오.

1. **공식 페이지 상단 요약란 미갱신** — 가장 자주 터집니다.
   상세페이지 요약란만 보지 말고 **실제 접수·예매가 이뤄지는 화면**을 교차
   확인하십시오. (세종 전시 야간개장 / 강원 EMS 서류 / 고비즈코리아 마감 /
   국립극장 매진 — 전부 이 유형)
2. **제목에 걸린 수치가 특정 날짜·요일에만 틀림** — 제목·요약·본문이 서로
   어긋나는지 반드시 대조.
3. **제출서류의 필수/선택 구분** — 공고에 없는 서류를 적었거나 필수를
   누락했는지. 선착순 사업이면 순위 상실로 직결됩니다.
4. **선착순·조기마감 안내 누락**.
5. **스포츠는 투수 이닝 합계 검산** — 합계가 경기 이닝과 안 맞으면 원고 오류.
6. **공고에 없는 내용을 "공고에 따르면"으로 귀속**하지 않았는지.
7. **입찰은 재공고·정정 여부**가 기사에 반영됐는지.

원출처를 못 열면 **UNVERIFIED**로 두십시오. 형식상 PASS 로 넘기면 안 됩니다.
(8/13 에 판정기 장애로 8편이 검증 없이 PASS 로 흘러간 적 있습니다.)

## 7. 최근 도입된 것 (2026-08-19~21)

- `reporting: direct|desk|sponsored|wire` + `reportingType` (direct 전용).
  8/28 부터 누락 시 빌드 실패. 그 전까지는 경고만.
  집계: `node scripts/reporting-report.mjs`
  **8/21 실물 검증 완료** — 코덱스에 전달하기 전에 6가지 조합을 직접 돌려 확인했다:
  누락(유예 중 경고 / 강제일 이후 빌드 중단), direct에 reportingType 없음, direct 아닌데
  reportingType 붙음, 두 필드의 오타 — 전부 문서대로 동작한다. 코덱스도 정본·자동화에
  반영 완료 회신(8/21).
  ⚠️ **미래 날짜 원고는 게이트에 닿지 않는다.** 예약 스킵이 검증보다 먼저 일어나기
  때문이다. 발행되지 않으니 문제는 아니지만, 미래 날짜로 게이트를 테스트하면 "통과"로
  보인다 — 테스트는 반드시 현재 시각 이하 날짜로 할 것.
- 취재 방식 배지 — direct 기사 바이라인에만 표시. **AI 관여도 표시가 아닙니다.**
- 광고(sponsor) 기사는 '많이 본' 랭킹 풀에서 제외됨.
- `metaDescription()` (`src/lib/seo.ts`) — summary 가 짧으면 본문 앞 문장을
  붙여 메타 태그만 확장. **기사 본문·summary 는 건드리지 않습니다.**
- `/newsroom` 편집국 소개, 기자 프로필 사진·전문분야·윤리 안내.
- 초기 하드코딩 데모 기사 48편 제거 — 기사 원천은 `content/articles/*.md` 뿐.
- **홈 서브리드 = 사업 축 2 + 종합뉴스 2**(8/21). 리드·속보 티커는 종합뉴스 전용 유지.
  `getSubLeads`(`src/lib/queries.ts`)의 `pickVaried()` 가 카테고리 중복을 피해 뽑는다 —
  하루 24편이 카테고리별 2편씩 같은 시각으로 들어와, 최신순 slice 만 쓰면 같은 축 2건이
  나란히 걸린다.
- `/policy` 7절 **AI 활용 고지**(8/21) — 기사 하단 배너가 가리키는 근거 문서.
  문구를 고칠 때는 배너 문구(`article/[slug]/page.tsx`)와 **같이** 봐야 한다.
- **TS2590 항구 조치**(8/25) — 기사가 1,085편을 넘자 `tsc`가 `content.generated.ts`의
  배열 리터럴에서 "union type too complex"로 빌드를 끊었다(1,061편이던 8/24까지는
  통과). `scripts/build-content.mjs`의 `header()`가 이제 `@ts-nocheck`를 파일 맨 위에
  쓴다 — 기계 산출물이라 내부 검사가 불필요하고, 내보내는 `Article[]` 타입은 그대로라
  소비처 검사는 유지된다. 기사 수는 계속 느니 지우면 재발한다.

## 8. 하지 말 것

- **없는 사실을 만들지 마십시오.** 기자 사진·경력, "○○에 확인한 결과" 같은
  취재 문구는 실제 근거가 있을 때만. 실명 가장은 되돌릴 수 없는 리스크입니다.
- **기사 본문·summary 를 임의로 늘리지 마십시오.** 편집 영역입니다.
- **과거 기사에 reporting 을 소급 추정하지 마십시오.** 없으면 unknown.
- **기사배열책임자 페이지를 만들지 마십시오.** 신문법 제10조는 인터넷뉴스
  서비스사업자 의무이고 인터넷신문사업자는 대상이 아닙니다(법령 원문 확인함).
- **`slug`·이미지 경로를 바꾸지 마십시오.** URL 은 파일명에서 나옵니다.

## 8-b. 발행 이력

- **2026-08-31** — 패키지 24편 인수, **24편 전부 발행 / HOLD 0 / 수정 0건.**
  조건부 PASS 15편의 게이트를 원출처에서 전부 열었고 **이번엔 고칠 것이 없었다.**
  ⚠️ **이틀 연속 틀렸던 grants 마감시각 유형이 재발하지 않았다.** 시각을 모르는
  건에서 원고가 시각을 주장하지 않았다(부산 수산물: `eventEndsAt` 만 23:59,
  제목·본문은 "9월 6일까지"). 8/29·8/30 지적이 반영된 것으로 보인다.
  원고가 오히려 집계 사이트보다 정확했던 사례 둘 —
  ```
  NIA      ALIO 는 "접수 시작 8/31"  → 원고는 "공고 게시 8/31, 접수 9/7 10:00" (공고문이 맞다)
  상록수역  K-Startup 요약란 만 20세 → 상세 조항 만 18세, 원고가 충돌을 본문에 밝힘
  한국외대  제안서 9/10 15:00 과 전자투찰 17:00 을 분리 (나라장터 투찰 17:00 과 일치)
  ```
  ⚠️ **발행 전 `git fetch` 를 지켰다.** 8/30 충돌 이후 규칙으로 넣은 것이다.
  direct 7일 이동 **0.6%** — 8/26 인터뷰 1편만 남았다.
- **2026-08-30** — 패키지 24편 인수, **24편 전부 발행 / HOLD 0**. 조건부 PASS 15편의
  게이트를 전부 원출처에서 열어 **2건 보정**.
  ```
  KEAD 채용     "기간제 3명" → 실제는 기간제 1 + 대체근로자(체험형 청년인턴) 2
  대구 동구     공고에 없는 마감 시각을 요약이 "오후 6시까지"로 단정 → 삭제
  ```
  ⚠️ **접수 시각은 양방향으로 틀린다.** 8/29 는 공고에 **있는** 시각을 "없다"고 했고,
  8/30 은 **없는** 시각을 "있다"고 했다. 실제 접수 화면에서 확인하는 수밖에 없다.
  ⚠️ **다른 개발자가 같은 저장소에 5커밋을 올렸다**(편집품질 8.5·기자검토 게이트 ·
  IndexNow 키 교체 · robots 라우트). 내 배포가 먼저 나가 푸시가 거부됐고, 병합 후
  재빌드·재배포해 양쪽을 합쳤다(`779500b`). 충돌 0건, 24편 전부 새 게이트도 통과.
  **발행 전 `git fetch` 로 원격을 먼저 볼 것.**
  direct 7일 이동 **0.6%** — 8/26 인터뷰 1편만 남았다.
- **2026-08-29** — 패키지 24편 인수, **24편 전부 발행 / HOLD 0**. 게이트에서 **5건 보정**.
  코덱스 판정은 PASS 24·HOLD 0 이었으나 원출처 재대조에서 **지원사업 2편의 마감 시각이
  실제와 달랐다.** 그대로 나갔으면 신청자가 마감을 놓친다.
  ⚠️ **둘 다 §6-1 유형** — 코덱스가 기업마당(bizinfo) 공고만 보고 "마감 시각 미명시"로
  처리했는데, **실제 접수가 이뤄지는 화면**에는 둘 다 시각이 있었다.
  ```
  인천 항공산업  BizOK 9/11 18:30 명시  → eventEndsAt 23:59 → 18:30
  울산 이차전지  UTP   9/18 18:00 명시  → eventEndsAt 23:59 → 18:00
  ```
  인천은 선정 규모도 틀렸다("공고가 제시하지 않았다" → BizOK 화면엔 5개사).
  두 공식 화면 표기가 다르다는 사실을 함께 적었다(§8-g).
  나머지 3건: 피노 집필지침 유출 1 · KBO 재편성 원칙 보강 1 · 출처 보강 1.
  ⚠️ **집필지침 게이트가 못 잡는 형태가 나왔다** — "이번 결정을 인수 완료로
  표현해서는 안 된다". 주어가 생략돼 `기사`·`제목`·`본문` 어느 단어도 없다.
  게이트 확장은 오탐 위험이 커서 보류했다. 발행 전 수동 sweep 을 계속할 것.
  ⚠️ **direct 7일 이동 2.4% → 0.6%.** 8/22 의 direct 3편이 창 밖으로 빠지고
  8/26 인터뷰 1편만 남았다. 새 direct 가 없으면 다음 주 0%다.
  재대조 이상 없던 것: 입찰 2(나라장터 차수·일정·금액·첨부) · DART 2(정정·철회 없음) ·
  MMCA 2 · KBO 2(1000만 관중 7개 수치 전부 일치) · WHO 1(PHEIC 유지).
  모바일 4조합 스모크 PASS.
- **2026-08-28 (2차 세션)** — **발행 없음**(출고대기에 신규 패키지 0). 감사 §4 조치를 실행했다.
  지침 유출 13편 수리 + 빌드 게이트 3층 · 본문 KST·취재로그 42편 정리 ·
  `reporting-report.mjs` 법정 기준 병기 + 내부 목표 10% · `05-direct-reporting.md` 폐기 ·
  코덱스 전달문 `코덱스_전달_20260828.md` 작성.
  **배포·라이브 검증 완료** — 변경 기사 52편 전수 확인(HTTP 200 · 지침문 0 ·
  본문 KST 0, 의미상 남긴 3편 제외). 빌드 산출물 1,157편 전수에도 지침문 0.
  ⚠️ **코덱스 회신 대기**: 발행 시각 분산 · 리드 문단 유지 · 소제목 어휘 통일 3건은
  원고 규약이라 코덱스가 정본에 반영해야 끝난다.
- **2026-08-28** — 패키지 24편 인수, **24편 전부 발행 / HOLD 0**.
  동적 게이트에서 24편 전부 변경없음 — 게이트 도입 이래 처음으로 한 건도 안 걸렸다.
  **오늘부터 `reporting` 필수화가 발효**됐고(누락 시 빌드 실패) 24편 전부 갖춰 정상 통과.
  ⚠️ direct 7일 이동 **6.1% → 2.4%**. 수치가 는 게 아니라 8/21 의 direct 6편이 7일 창
  밖으로 빠져서다. 내일이면 8/22 의 3편도 빠진다 — 새 direct 가 없으면 이번 주 안에 0%대.
- **2026-08-27** — 패키지 24편 인수, **23편 발행 / 1편 HOLD**.
  핀테크위크 데모데이: 필수 제출서류 4번이 **첨부 공고문(4대 사회보험 명부)과 포털 본문
  (법인등기부등본)에서 서로 다름**. 원고가 첨부만 따랐다. 실제 접수가 이뤄지는 포털이 정본.
  코덱스가 사전에 HOLD·교체한 3건(KOPRI 접수엔진 점검·화성 제출방식 충돌·메시아 러닝타임
  충돌)의 원본도 보류자료로 꺼냈다. 코덱스 전달문: `modooilbo-packages/코덱스_전달_20260827.md`
  직접취재 7일 이동 **6.1%**.
- **2026-08-26** — 현성전자 서면인터뷰 1편 추가 발행(direct/interview). 조건부 PASS 4건 중
  3건을 검수에서 해제 — 특히 제목의 "삼성 온라인 공식 파트너"는 **오프라인 매장 목록이 아니라
  https://www.samsung.com/sec/sop/ (온라인 공식 파트너 안내)** 에서 확인된다. 코덱스가 못 찾은
  이유가 이것이다. 이해관계(브리찌–현성전자, 발행인 관여)는 기사 하단에 공개, sponsor 미사용.
- **2026-08-26** — 패키지 24편 인수, **24편 전부 발행 / HOLD 0**. 코덱스 최초 판정은
  PASS 15·조건부 PASS 9(최초 HOLD 15편을 RAC/RBC/RCC/RDE/REC/RFG 6개 독립재검수로 전부
  교정·해소). 발행 직전 라이브 동적 게이트(12개 카테고리 병렬 재대조)에서도 24편 전부
  PASS — 지원·입찰 4건 접수상태·최고차수(000, 001/002 미존재 직접 조회)·첨부, 창업 2건
  자격-첨부 충돌 고지, 성별균형 공모전 지정양식, 채용 2건 정정공고, SK합병·우원개발
  DART 후속공시, 문화 2건 예매연결·회차, KBO 2경기 이닝별 득점 검산까지 확인.
  김창완밴드 공연(러닝타임 120분/70분 표기 충돌 미해소)은 보류자료로 격리, 같은 자리는
  "마음을 담은 클래식"으로 교체 발행. 당시에는 직접취재도 자동승인 대상이었으나,
  **그 운영은 2026-09-01 취소됐습니다**(맨 위 긴급 정정 우선).
- **2026-08-25** — 패키지 24편 인수, **24편 발행 / 게이트에서 2건 보정**.
  (싱가포르 7월 물가: 원문에 없는 "계절조정 기준" 귀속을 제거 — 부록 지수 재계산
  102.172→102.463=+0.3%로 실제로는 비계절조정 수치였음 / ICT 규제샌드박스: "9시 접수
  개시 예정"의 예고형을 "접수 진행 중"으로 재작성 — 발행 시점엔 이미 접수가 시작돼 있었음).
  빌드가 TS2590(유니언 타입 한계 초과, 기사 1,085편에서 실측)으로 한 번 끊겼다 —
  `scripts/build-content.mjs`의 `header()`에 `@ts-nocheck`를 추가해 항구 조치함(§7 참고).
- **2026-08-24** — 패키지 24편 인수, **24편 전부 발행 / HOLD 0**.
  변경 1건(HYPNOSIS 스탠딩석 184→183, 295석 중 1석)은 **HOLD 하지 않았다** — 원고가
  "24일 오전 6시 14분 기준"을 본문에 명시하고 제목에 수치를 넣지 않아 서술이 여전히 참이다.
  §8-d 의 "제목에 변동값 금지"가 지켜져 사흘 걸리던 유형이 사라졌다.
  직접취재 7일 이동 **9.8%** — `direct 0편` 사흘 연속. 목표의 절반 아래.
- **2026-08-23** — 패키지 24편 인수, **21편 발행 / 3편 HOLD**
  (오이디푸스: 제목 잔여석 85·212 → 78·175 + 오후 2시 회차 온라인 예매 이미 마감 /
   모케 태풍: 공보 10호 → 11호 갱신으로 6개 값 변동 / 빠씨: 690 → 604석).
  직접취재 7일 이동 **13.2%** — `direct 0편`, 이틀 연속 미달. 다음 배치에 실제 회신 필요.
  ⚠️ **반박검증이 오탐 2건을 걷어냈다.** 1차 검증이 치명으로 올린 두 건이 실제로는
  원고가 맞았다 — 변경 주장은 반드시 독립 재확인할 것(아래 §8-c).
- **2026-08-22** — 코덱스 패키지 24편 인수, **23편 발행 / 1편 HOLD**(이은결 공연:
  개막일 잔여석을 "미공개"로 단정했으나 라이브는 16석 공개. 회차코드 오조회가 원인).
  HOLD 원고와 사유는 `~/GIT/modooilbo-packages/출고대기/보류자료/`.
  직접취재 7일 이동 **19.1%** — 목표 20~30% 미달, 다음 배치에서 direct 1편이면 회복.
- 2026-08-21 — 24편 발행.

⚠️ **나라장터 공고는 WebFetch 로 검증되지 않는다.** 상세가 SSO 로그인을 요구해
브라우저·curl 모두 302 로 튕긴다. 반드시 `node scripts/check-bid.mjs <공고번호>` 를 쓸 것.
8/22 에 이것 때문에 멀쩡한 기사가 "확인불가"로 잡혔다.

## 8-c. 동적 게이트에서 반복되는 오탐 (변경 주장은 반드시 재확인)

1차 검증이 "변경됐다"고 해도 그대로 HOLD 하지 말 것. 8/23 에 치명 2건이 오탐이었다.

- **보도자료는 웹 본문과 첨부가 다르다.** korea.kr 본문에 없다고 "원문에 없는 수치"로
  단정하면 안 된다. 첨부 hwp·pdf 를 내려받아 확인할 것. (8/23 수상구조대 사설)
- **세션 기반 사이트는 진입 경로에 따라 화면이 다르다.** 메인을 안 거치고 딥링크하면
  "접수기간이 아닙니다" 같은 폴백이 뜬다. 독자가 겪는 화면이 아니다. (8/23 거브테크)
- **NHC 태풍은 CurrentStorms.json 이 캐시 지연된다.** 텍스트 공보(text/*.shtml)가 빠르다.
  공보 번호부터 대조할 것.

## 8-g. 첨부와 웹 본문이 다르면 — 둘 다 보고, 다르다는 사실을 쓴다

이번 주에 **양방향으로** 걸렸다.

```
8/23  첨부를 안 봐서   "원문에 없는 수치"라고 오판할 뻔함 (수상구조대 사설 — 오탐)
8/27  첨부만 보고      웹 본문을 놓침                     (핀테크위크 — 실제 오류)
```

정본은 **실제 접수·예매가 이뤄지는 화면**이다. 다만 4번 항목만 고치고 끝내면 안 된다 —
두 공식 문서가 다르다는 사실 자체가 독자에게 필요한 정보다. 신청자는 둘 다 준비하거나
주최측에 확인해야 하기 때문이다.

## 8-e. 변동값 HOLD 판정 — 기계적으로 적용하지 말 것

"변동됐으니 HOLD"를 그대로 적용하면 **시각을 명시한 좋은 서술까지 죽인다.**
판단 기준은 *지금 값과 다른가*가 아니라 **원고의 주장이 아직 참인가**다.

```
거짓 → HOLD   제목이 무조건부로 "690석"인데 실제 604석        (8/23 빠씨)
거짓 → HOLD   제목이 "85·212석"인데 78·175석 + 예매 마감 누락  (8/23 오이디푸스)
참   → 발행   본문이 "6시14분 기준 184석"이고 지금 183석      (8/24 HYPNOSIS)
```

## 8-d. 제목에 변동값을 박지 말 것

잔여석·재고처럼 분 단위로 변하는 수치를 제목에 넣으면 **발행 순간 이미 틀린다.**
8/22 이은결, 8/23 오이디푸스·빠씨가 전부 이 유형이었다(3일 중 3일).
넣어야 하면 기준 시각을 제목에 함께 적는다.

⚠️ **예매 API 의 빈 배열을 상태로 읽지 말 것.** 세종문화회관 엔진에서 매진은 빈 배열이
아니라 0 으로 채운 리스트다. 빈 배열은 "그 파라미터 조합의 회차 없음"이다.
회차코드는 요일이 아니라 그날의 실제 회차 구성으로 정해진다.

## 8-f. 헤더 유리(리퀴드글라스) — 2026-08-21 작업

헤더는 **통유리 한 장**이다. `backdrop-filter` 요소가 헤더 영역에 **1개만** 있어야 한다.

```
layout.tsx  .sticky.top-0.z-40   ← 여기서만 유리를 건다
   Header(유틸바·마스트헤드·GNB 2행) · BreakingTicker · TrendingTags = 전부 투명
값: blur 12px · 흰 30%(다크 30%) · 폴백 불투명 95%
```

⚠️ **자식에 `background` 나 `backdrop-filter` 를 넣지 말 것.** 각자 제 백드롭을 따로
필터링해 층이 갈리고 유리가 여러 장으로 보인다. 구분선은 `border` 로만 넣는다.

⚠️ **테일윈드 opacity 는 5단위다.** `/48` `/62` 같은 값은 클래스가 아예 생성되지 않고
조용히 무시된다(빌드·타입 통과, 화면만 안 바뀜). 반드시 5의 배수로.

⚠️ **`backdrop-filter` 를 헤드리스 스크린샷으로 판정하지 말 것.** Playwright 헤드리스는
blur 를 적용하지 않는다. `scripts/shoot.mjs` 도 헤드리스라 같은 함정이 있다.
유리·블러 확인은 `chromium.launch({channel:"chrome"})` 로 찍어야 한다.

GNB 2행은 `--gnb-col`(layout 아닌 Header wrapper 한 곳에서 정의) 6칸 그리드로
위·아래가 같은 컬럼에 중앙정렬돼 있다. 폭을 한쪽만 바꾸면 정렬이 깨진다.

## 9. 자주 쓰는 확인

```bash
# 라이브 HTTP 확인. UA 지정은 차단 증상을 재현·우회할 때만 선택적으로 사용한다.
curl -sS -o /dev/null -w "%{http_code}\n" -H "Cache-Control: no-cache" \
  https://modooilbo.com/article/<slug>/

# 나라장터 / DART
node scripts/check-bid.mjs R26BK0XXXXXXX
curl -s -A "Mozilla/5.0" "https://dart.fss.or.kr/dsaf001/main.do?rcpNo=<14자리>"

# R2 확인 캐시는 Git common dir의 modooilbo-cache/r2-synced.json을 모든 worktree가 공유합니다.
# 쓰기 직전 공용·현재·기본 worktree 레거시 캐시를 잠금 안에서 재병합하고 원자 교체합니다.
# Git 밖에서는 gitignore 된 scripts/.r2-synced.json으로 폴백하며, 어느 경로든 속도 캐시일 뿐입니다.
# 지워도 다음 실행에서 HEAD 재검사하며, 강제 전수 확인은 --verify-all을 사용합니다.
```

`scripts/ping-indexnow.mjs`의 **현재 정본 동작**은 RSS 최신 기사 최대 30개와 홈을 모아
Bing/IndexNow와 Naver에 병렬 통지하는 것입니다. 응답 코드는 실행 시점에 달라질 수 있으므로
로그에 나온 엔진별 HTTP 상태를 그대로 기록하십시오.

```
[indexnow] 31개 URL 통지 → Bing/IndexNow HTTP <상태> · Naver HTTP <상태>
```

⚠️ **키 파일 두 개를 모두 남겨 두십시오.** `public/df645cf5….txt`(옛 32자)와
`public/fc30393c….txt`(새 64자)가 둘 다 서비스 중입니다. 옛 키를 지우면 그 키로
검증해 둔 쪽이 깨질 수 있습니다. 종전 경고("키를 바꾸지 마라")는 이 커밋으로
무효가 됐지만, **키를 또 갈 이유는 없습니다.**

⚠️ HTTP 200·202는 요청 접수 증거일 뿐 `indexed_current` 증거가 아닙니다. 통지 목록은
RSS 최신 기사 최대 30개 + 홈뿐이므로, 신규 발행 canonical URL 전건은 §5의 색인 담당자에게
별도로 인계합니다. `/policy`·`/about` 같은 정적 페이지는 자동 목록에 포함되지 않습니다.
